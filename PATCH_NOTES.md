# SchoolPro patch notes

تم تطبيق حزمة إصلاحات للصفوف، الطلاب، المدرسين، الجدول، الحضور، الامتحانات، الدرجات، الأقساط، الثيم، والطباعة.

## فحوصات تم تشغيلها

- `npm run lint` — بدون تحذيرات أو أخطاء.
- `npx tsc --noEmit` — بدون أخطاء TypeScript.
- `npm run build` — وصل إلى `Compiled successfully` ثم انتهت المهلة في بيئة التنفيذ أثناء مرحلة فحص Next الداخلية؛ فحص lint وTypeScript منفصلين مرّا بنجاح.

## ملاحظات مهمة

- القاعات أضيفت كاختيارات/اقتراحات محفوظة من القاعات السابقة داخل الجدول، وليست صفحة إدارة قاعات مستقلة لأن قاعدة البيانات الحالية لا تحتوي جدول قاعات منفصل.
- صلاحيات حساب مدرس مستقل تحتاج توسعة نظام المستخدمين/الأدوار الحالي، لذلك لم تُضاف حتى لا ينكسر تسجيل الدخول الإداري الحالي.
